apiVersion: v1
kind: pod
metadata:
         "name": minal
spec: 
          containers: 
                  -image: tomcat:latest
                    name: tomcatpod
                         resources: 
                         request: 
                          cpu: "500m"
                          memory: "128Mi"
                  port:
                      -name: http
                       containerPort: 80
                       protocol: TCP         
                      
                           